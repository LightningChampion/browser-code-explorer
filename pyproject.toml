[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "browser-code-explorer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["playwright>=1.50,<2"]

[tool.setuptools.packages.find]
include = ["explorer*"]
